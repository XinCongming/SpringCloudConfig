server:
  port: 8001
spring:
  application:
    name: product-1
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka,http://localhost:8761/eureka
  instance:
    prefer-ip-address: true   #使用ip作为host
#product-1
#  ribbon
#    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule  #更改负载均衡的策略 RandomRule随机
